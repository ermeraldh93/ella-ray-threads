const menu=document.querySelector('.menu'),nav=document.querySelector('.nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.getElementById('quoteForm')?.addEventListener('submit',e=>{e.preventDefault();const f=e.currentTarget;const msg=f.querySelector('.form-message');const data=new FormData(f);const subject=encodeURIComponent('New Ella Rae Threads Quote Request');const body=encodeURIComponent([...data.entries()].filter(([k,v])=>k!=='logo').map(([k,v])=>`${k}: ${v}`).join('
'));window.location.href=`mailto:Ellaraethreads@gmail.com?subject=${subject}&body=${body}`;msg.textContent='Opening email app. Supabase quote storage can be connected next.';});